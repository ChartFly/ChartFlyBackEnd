# === Database Connection Pieces ===
DB_HOST=your-neon-db-host
DB_NAME=chartflydatabase
DB_USER=your-db-username
DB_PASS=your-db-password
DB_PORT=5432
DB_SSL=require

# === Developer Reset Token (used in dev reset route only) ===
DEV_RESET_TOKEN=your-dev-reset-token

# === Session Secret (Split into parts) ===
SESSION_PREFIX=your-session-prefix
SESSION_SUFFIX=your-session-suffix

# === Email Configuration (Modular format) ===
EMAIL_USER=your-email-name
EMAIL_DOMAIN=gmail.com
EMAIL_APP_PASS=your-app-password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587